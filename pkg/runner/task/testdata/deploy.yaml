apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: applytest
  labels:
    name: nginx
spec:
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx
